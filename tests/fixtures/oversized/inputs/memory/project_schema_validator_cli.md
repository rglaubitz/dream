---
name: Schema Validator CLI
description: Standalone validator CLI. Runs pre-ingest against schema registry. 62 tests.
type: project
---

Validates incoming document payloads against the schema registry before they enter the ingest queue. Rejects malformed payloads with structured error output.

**Status:** Deployed. 62 tests passing.

**Commands:** validate, explain, list-schemas.
